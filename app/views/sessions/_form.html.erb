<%= form_with url: sessions_path, local: true do |form| %>
<% flash_messages.each do |v| %>
    <%= content_tag :p, v, class: 'flash-alert'%>
  <% end %>
  <p>
    <%= form.label :email %>
    <%= form.text_field :email, id: :email %>
  </p>

  <p>
    <%= form.label :password %>
    <%= form.password_field :password, id: :password %>
  </p>

  <p><%= form.submit 'Log In'%></p>
<% end %>
